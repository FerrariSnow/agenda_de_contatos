class Contato {
  nome: string
  phone: number
  email: string
  id: number

  constructor(nome: string, phone: number, email: string, id: number) {
    this.nome = nome
    this.phone = phone
    this.email = email
    this.id = id
  }
}

export default Contato
